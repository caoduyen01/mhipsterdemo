apply plugin: "war"

bootWar {
    mainClassName = "com.micronaut.app.MicronautApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
